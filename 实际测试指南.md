# 实际测试指南

**日期**: 2024-12-02  
**测试目标**: 验证传统逻辑分析集成后的占卜功能

## ✅ 集成测试结果

### 核心功能测试
- ✅ **铜钱占卜流程**: 通过
  - 占卜执行正常
  - 动爻计算正确
  - 传统逻辑分析生成成功
  - 数据结构完整

- ✅ **梅花易数流程**: 通过
  - 占卜执行正常
  - 传统逻辑分析生成成功
  - 数据结构完整

- ✅ **数据结构验证**: 通过
  - 卦宫数据完整
  - 体用关系完整
  - 六亲信息完整
  - 动爻分析完整

## 🌐 浏览器测试步骤

### 前置准备

1. **启动开发服务器**
   ```bash
   npm run dev
   ```

2. **确认访问地址**
   - 通常是: `http://localhost:5173` 或类似地址

### 测试场景

#### 场景1: 铜钱占卜测试

**步骤**:
1. 访问六爻占卜页面（`/divination` 或 `/hexagram-divination`）
2. 选择"铜钱占卜法"
3. 输入一个问题，如："我应该跳槽吗？"
4. 点击"掐指一算"按钮
5. 观察铜钱投掷动画
6. 等待AI解读生成

**验证点**:
- ✅ 铜钱动画正常播放
- ✅ 卦象正确显示（本卦和变卦）
- ✅ AI解读生成成功
- ✅ 控制台检查：是否有 `traditionalAnalysis` 数据
- ✅ AI解读内容是否基于传统逻辑（检查是否提到体用关系、卦宫等）

**检查方法**:
1. 打开浏览器开发者工具（F12）
2. 查看 Console 标签页
3. 查找包含 "traditionalAnalysis" 的日志
4. 查看 Network 标签页，检查 AI API 请求的 payload，确认包含传统逻辑分析

#### 场景2: 梅花易数测试

**步骤**:
1. 访问六爻占卜页面
2. 选择"梅花易数法"
3. 选择"数字起卦"
4. 输入两个数字（如：3, 5）
5. 输入一个问题
6. 点击"掐指一算"按钮
7. 等待结果

**验证点**:
- ✅ 梅花易数计算正常
- ✅ 卦象正确显示
- ✅ 传统逻辑分析生成（体用关系应为：上卦用、下卦体）
- ✅ AI解读生成成功

#### 场景3: 验证传统逻辑分析内容

**在浏览器控制台执行**:

```javascript
// 执行一次占卜后，在控制台检查结果
// 假设结果存储在某个变量中，或从组件中检查

// 检查是否有传统逻辑分析
console.log('传统逻辑分析:', divinationResult?.traditionalAnalysis);

// 检查体用关系
if (divinationResult?.traditionalAnalysis?.bodyUsage) {
  const bodyUsage = divinationResult.traditionalAnalysis.bodyUsage;
  console.log('体用关系:', {
    体卦: bodyUsage.bodyTrigram,
    用卦: bodyUsage.usageTrigram,
    关系: bodyUsage.relationship,
    解读: bodyUsage.interpretation.generalMeaning
  });
}

// 检查卦宫信息
if (divinationResult?.traditionalAnalysis?.palaceData) {
  console.log('卦宫信息:', divinationResult.traditionalAnalysis.palaceData);
}

// 检查动爻分析（如果有）
if (divinationResult?.traditionalAnalysis?.changingLinesAnalysis) {
  console.log('动爻分析:', divinationResult.traditionalAnalysis.changingLinesAnalysis);
}
```

### 测试检查清单

#### 功能检查
- [ ] 铜钱占卜可以正常执行
- [ ] 梅花易数可以正常执行
- [ ] 随机起卦选项已移除
- [ ] 占卜结果正常显示

#### 传统逻辑检查
- [ ] 铜钱占卜生成传统逻辑分析
- [ ] 梅花易数生成传统逻辑分析
- [ ] 体用关系计算正确
- [ ] 六亲信息完整
- [ ] 世应位置正确

#### AI解读检查
- [ ] AI解读生成成功
- [ ] AI解读包含传统逻辑内容（检查是否提到体用关系、卦宫等）
- [ ] AI解读语言现代、易懂
- [ ] AI解读不再显得"AI味十足"

#### 错误处理检查
- [ ] 网络错误时，传统逻辑分析仍保留
- [ ] AI服务失败时，有降级方案
- [ ] 错误提示友好

### 验证AI解读质量

**判断标准**:
1. **是否基于传统逻辑**: 
   - 检查解读是否提及体用关系、卦宫、五行等传统概念
   - 是否解释了体用生克的含义

2. **语言风格**:
   - 现代、易懂
   - 不过度使用传统术语堆砌
   - 有针对性建议

3. **逻辑一致性**:
   - 解读结论与体用关系一致
   - 没有自相矛盾的地方

**示例验证**:
```
✅ 好的解读示例:
"根据体用关系分析，体卦克用卦，表明您能够主动控制局面..."
"从卦宫五行来看，当前处于坎宫（水），需要..."

❌ 不好的解读示例（AI味十足）:
"建议您保持积极心态，收集更多信息..."
"这可能是一个机会，也可能是一个挑战..."
```

## 🔍 调试方法

### 1. 查看传统逻辑分析数据

在浏览器控制台执行：
```javascript
// 假设有全局变量存储结果
window.__lastDivinationResult
```

### 2. 检查AI提示词

查看 Network 标签页中的 AI API 请求：
- 找到 LLM API 请求
- 查看 Request Payload
- 确认包含传统逻辑分析内容

### 3. 检查数据流

```javascript
// 在组件中临时添加日志
console.log('占卜结果:', divinationResult);
console.log('传统逻辑分析:', divinationResult?.traditionalAnalysis);
```

## 📊 预期结果

### 成功的标志

1. **占卜流程顺畅**
   - 点击后立即开始
   - 动画流畅（铜钱法）
   - 结果正常显示

2. **传统逻辑分析完整**
   - 卦宫信息存在
   - 体用关系计算正确
   - 六亲信息完整
   - 动爻分析准确（如果有动爻）

3. **AI解读质量提升**
   - 解读包含传统逻辑基础
   - 不再显得"AI味十足"
   - 有具体的易经推演过程
   - 建议有针对性

### 可能的问题

1. **卦名不匹配**
   - 症状: 某些卦找不到卦宫数据
   - 解决: 已实现模糊匹配，应该能处理

2. **传统逻辑分析缺失**
   - 症状: `traditionalAnalysis` 为 undefined
   - 检查: 查看控制台错误日志

3. **AI解读仍显AI味**
   - 症状: AI解读缺乏传统逻辑基础
   - 检查: 确认AI提示词中包含传统逻辑分析

## 📝 测试报告模板

测试完成后，记录：

```
测试时间: __________
测试人员: __________

测试结果:
- 铜钱占卜: [ ] 通过 [ ] 失败
- 梅花易数: [ ] 通过 [ ] 失败
- 传统逻辑分析: [ ] 完整 [ ] 缺失
- AI解读质量: [ ] 好 [ ] 一般 [ ] 差

发现的问题:
1. 
2. 

改进建议:
1. 
2. 
```

---

**开始测试**: 运行 `npm run dev` 并访问占卜页面

